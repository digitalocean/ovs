Description: Upstream changes introduced in version 1.1.0~pre2-2
 This patch has been created by dpkg-source during the package build.
 Here's the last changelog entry, hopefully it gives details on why
 those changes were made:
 .
 openvswitch (1.1.0~pre2-2) unstable; urgency=low
 .
   * Clarify licensing of files in xenserver/
 .
 The person named in the Author field signed this changelog entry.
Author: Simon Horman <horms@debian.org>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: http://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: <YYYY-MM-DD>

--- openvswitch-1.1.0~pre2.orig/Makefile.in
+++ openvswitch-1.1.0~pre2/Makefile.in
@@ -657,8 +657,7 @@ AM_CPPFLAGS = $(SSL_CFLAGS) -I $(top_src
 	$(top_srcdir)/lib $(am__append_1)
 AM_CFLAGS = -Wstrict-prototypes $(WARNING_FLAGS) $(am__append_2)
 @NDEBUG_FALSE@AM_LDFLAGS = -export-dynamic
-ALL_LOCAL = check-vlog-modules $(am__append_9) \
-	check-debian-changelog-version
+ALL_LOCAL = check-vlog-modules $(am__append_9)
 BUILT_SOURCES = $(OVSIDL_BUILT)
 CLEANFILES = $(nodist_lib_libopenvswitch_a_SOURCES) $(lcov_wrappers) \
 	$(valgrind_wrappers)
@@ -778,7 +777,7 @@ EXTRA_DIST = CodingStyle INSTALL.KVM INS
 	xenserver/usr_share_openvswitch_scripts_sysconfig.template \
 	xenserver/uuid.py $(ovs_pyfiles) python/ovs/dirs.py
 bin_SCRIPTS = utilities/ovs-pki utilities/ovs-vsctl
-DIST_HOOKS = dist-hook-git check-debian-changelog-version
+DIST_HOOKS = dist-hook-git
 dist_man_MANS = vswitchd/ovs-vswitchd.conf.db.5
 dist_pkgdata_DATA = 
 dist_pkgdata_SCRIPTS = 
@@ -3033,15 +3032,6 @@ tests/idltest.c: tests/idltest.h
 @HAVE_PYTHON_TRUE@	$(PYTHON) $(srcdir)/build-aux/check-structs $(srcdir)/include/openflow/openflow.h $(srcdir)/include/openflow/nicira-ext.h
 @HAVE_PYTHON_TRUE@	touch $@
 
-check-debian-changelog-version:
-	@if $(FGREP) '($(VERSION))' $(srcdir)/debian/changelog >/dev/null; \
-	then								   \
-	  :;								   \
-	else								   \
-	  echo "Update debian/changelog to mention version $(VERSION)";	   \
-	  exit 1;							   \
-	fi
-
 $(srcdir)/debian/copyright: AUTHORS debian/copyright.in
 	{ sed -n -e '/%AUTHORS%/q' -e p < $(srcdir)/debian/copyright.in;   \
 	  sed '1,/^$$/d' $(srcdir)/AUTHORS |				   \
--- openvswitch-1.1.0~pre2.orig/datapath/Makefile.in
+++ openvswitch-1.1.0~pre2/datapath/Makefile.in
@@ -290,9 +290,9 @@ $(srcdir)/Makefile.in:  $(srcdir)/Makefi
 	      exit 1;; \
 	  esac; \
 	done; \
-	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign datapath/Makefile'; \
+	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu datapath/Makefile'; \
 	$(am__cd) $(top_srcdir) && \
-	  $(AUTOMAKE) --foreign datapath/Makefile
+	  $(AUTOMAKE) --gnu datapath/Makefile
 .PRECIOUS: Makefile
 Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
 	@case '$?' in \
